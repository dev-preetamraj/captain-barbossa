Use case: ui-mockup
Asset type: Captain Barbossa version 3, revised terminal UI concept.
Input image: attached v2 screenshot is the edit target. Retain dark monospace terminal feel and captain-below-crew layout, but radically simplify the architecture shown.
USER'S ESSENTIAL CORRECTION: This product has NO chat interface of its own. Each pane contains the actual interactive CLI application, either Codex CLI or Claude Code. Captain Barbossa only arranges real terminal panes. Native agent prompts and native agent output are the only conversations. Do not reproduce v2's custom chat system.

Create a single large terminal screenshot, landscape approximately 1800x1200. ONE application screen only, not side by side states. This is a minimal design concept, not a claim of a pixel-perfect live provider screenshot. Small discreet text outside the terminal at top left: "CAPTAIN BARBOSSA / V3". No slogan or further captions.

Layout: two equally wide live crew terminals in one row across the upper 52% of the terminal; one full-width captain terminal below occupying the lower 48%. Hairline slate terminal separators, no floating cards and no inset nested boxes. Broad empty terminal space naturally surrounds the sparse live output. Consistent crisp monospace. Matte near-black flat background, warm off-white text, gray secondary text, very small soft mint accent for the focused captain separator. No glow, textures, gradients, shadows or 3D.

The ONLY application-owned UI is an unobtrusive lowercase label embedded at each pane's top border, and one tiny footer hint. Pane labels exactly:
upper left: "builder · codex"
upper right: "reviewer · claude"
bottom: "captain · claude"

Everything inside these boundaries is the native agent terminal. Distinguish native Codex output and Claude output through their ordinary terminal prompt/bullet idioms. No portraits, avatars, speech bubbles, sender headings, chat cards, pill tags, buttons, custom input bars, recipient placeholders, tabs, overview dashboard, task labels, metadata panels, running/done badges, global project header, or repeated explanations.

UPPER LEFT Codex native terminal contents, aligned left with native terminal padding:
"› Implement rate limiting."
(blank line)
"• Updating src/limits.py"
"  Added per-user request windows."
(blank lines)
"›"
This empty native prompt has NO visible cursor, because not focused.

UPPER RIGHT Claude native terminal contents:
"❯ Review the rate limiter."
(blank line)
"⏺ Read(src/limits.py)"
(blank line)
"⏺ Checking boundary conditions."
(blank lines)
"❯"
No visible cursor.

LOWER FULL-WIDTH Captain Claude native terminal contents:
"❯ Add rate limiting and review the change."
(blank line)
"⏺ Builder is implementing. Reviewer will check the result."
(two blank lines)
"❯" followed by one small mint block caret. This is Claude's own plain prompt, not a custom input field.
Leave generous remaining native terminal whitespace without inventing content.

Bottom outside the three panes, one very muted compact help line:
"Ctrl+B  then  arrows: focus · z: zoom"
That is the only shortcut line. Zoom means the selected real terminal expands full-screen; no separate expanded state in this image.

Key difference from reference: one terminal, two actual crew CLI panes and one actual captain CLI pane. No application-created conversations anywhere. No separate author names "You"/"Captain" inside output. Reduce information density by at least 60%, with calm alignment and natural terminal whitespace. Native agent output must be legible, clean and simple.
