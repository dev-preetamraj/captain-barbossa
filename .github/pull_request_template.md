## Change

Describe the problem and the resulting behavior. Link a related issue if there is one.

## Validation

- [ ] `uv run --locked pre-commit run --all-files --hook-stage pre-push` passes.
- [ ] Relevant tests and documentation are updated.

For terminal or agent-launch changes, describe any manual checks, including the
OS, Herdr version, and agent CLI. Say when native startup was not tested.
